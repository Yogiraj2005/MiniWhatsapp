<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit chat</title>
</head>
<body>
    <p>Edit your chat: <%= chat._id %></p>
    
    <p>this chat is sent from <b><%= chat.from %> </b>to <%= chat.to %></p>

    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">        
        <textarea name="msg" rows="10" cols="20"><%= chat.msg %></textarea>
        <button>Edit</button>
    </form>
</body>
</html>